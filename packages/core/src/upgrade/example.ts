import { ProjectUpgrade } from '../type/project.js';

/**
 * This is an example implementation of a upgrade file
 */
const upgrade: ProjectUpgrade = {
  coreVersion: '0.0.0',
  run: async (project) => {},
};

export default upgrade;
